#include "main.h"
/**
*_abs - returns the absolute of the input
*@a: it is an integer
*Description: returns the absolute of the input'
*Return: 0
*/
int _abs(int a)
{
if (a >= 0)
{
return (a);
}
return (-a);
}
